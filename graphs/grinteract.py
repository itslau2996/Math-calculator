from tkinter import messagebox


def grint(m):
    if m == "A":
        messagebox.showinfo("Error", "Still in development, thus disabled")
    elif m == "B":
        messagebox.showinfo("Error", "Still in development, thus disabled")
    elif m == "C":
        messagebox.showinfo("Error", "Still in development, thus disabled")
    elif m == "D":
        messagebox.showinfo("Error", "Still in development, thus disabled")
